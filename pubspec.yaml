name: gastos_general
description: "App de control de gastos personales."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  intl: ^0.19.0

flutter:
  uses-material-design: true
